<project name="nuxeo-assembly" default="build" xmlns:nx="urn:nuxeo-build" xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml" uri="urn:nuxeo-artifact" />
  <taskdef resource="net/sf/antcontrib/antlib.xml" />
  <target name="init" unless="init.done">
    <property name="outdir" value="${maven.project.build.directory}" />
    <antcall target="expand" />
    <property name="init.done" value="true" />
  </target>

  <target name="expand">
    <artifact:nuxeo-expand includeTestScope="true" groupPrefixes="org.nuxeo,nuxeo-studio" />
    <artifact:print output="${outdir}/artifacts-easyshare.properties" mode="sdk" />
    <artifact:print output="${outdir}/test-artifacts-easyshare.properties" mode="sdk" scopes="test" />
    <artifact:print output="${outdir}/dependency-tree.log" />
  </target>

  <target name="build" depends="init" description="Build  Marketplace package">
    <tstamp />
    <delete failonerror="false" dir="${outdir}/nxr" />
    <mkdir dir="${outdir}/nxr" />
    <delete failonerror="false" dir="${outdir}/marketplace" />
    <mkdir dir="${outdir}/marketplace" />
    <copy todir="${outdir}/marketplace">
      <fileset dir="src/main/resources" />
      <filterset>
        <filter token="VERSION" value="${marketplace.easyshare.version}" />
        <filter token="DISTRIB_VERSION" value="${nuxeo.distribution.version}" />
      </filterset>
    </copy>
    <copy file="${outdir}/artifacts-easyshare.properties" todir="${outdir}/marketplace/install" />
    <copy file="${outdir}/test-artifacts-easyshare.properties" todir="${outdir}/marketplace/install" />

    <!-- Base distribution NXR -->
    <copy todir="${outdir}">
      <artifact:file key="org.nuxeo.ecm.distribution:nuxeo-nxr-server::zip" />
      <artifact:file key="org.nuxeo.ecm.distribution:nuxeo-nxr-jsf-ui::zip" />
    </copy>

    <!-- Nuxeo bundles -->
    <copy todir="${outdir}/nxr/bundles" overwrite="true">
      <artifact:set>
        <includes>
          <artifact groupId="org.nuxeo*" />
          <artifact groupId="nuxeo-studio" />
        </includes>
        <excludes>
          <artifact groupId="org.nuxeo.lib*" />
          <artifact scope="test" />
          <artifact scope="provided" />
          <artifact type="pom" />
        </excludes>
      </artifact:set>
    </copy>
    <!-- Third-party libraries -->
    <copy todir="${outdir}/nxr/lib" overwrite="true">
      <artifact:set>
        <includes>
          <artifact groupId="!org.nuxeo*" scope="!test" />
          <artifact groupId="org.nuxeo.lib*" scope="!test" />
        </includes>
        <excludes>
          <artifact groupId="nuxeo-studio" />
        </excludes>
      </artifact:set>
    </copy>
    <nx:rmdups dir="${outdir}/nxr/lib" />

    <unzip src="${outdir}/nuxeo-nxr-server-${nuxeo.distribution.version}.zip"
           dest="${outdir}/nuxeo-nxr-server-jsf-ui" />
    <unzip src="${outdir}/nuxeo-nxr-jsf-ui-${nuxeo.distribution.version}.zip"
           dest="${outdir}/nuxeo-nxr-server-jsf-ui" />
    <zip destfile="${outdir}/nuxeo-nxr-server-jsf-ui-${nuxeo.distribution.version}.zip"
         basedir="${outdir}/nuxeo-nxr-server-jsf-ui" />
    <zip destfile="${outdir}/nxr-${marketplace.easyshare.version}.zip" basedir="${outdir}/nxr" />
    <!-- Diff against Nuxeo Server + JSF UI -->
    <nx:zipdiff file1="${outdir}/nxr-${marketplace.easyshare.version}.zip"
                file2="${outdir}/nuxeo-nxr-server-jsf-ui-${nuxeo.distribution.version}.zip"
                includesfile="${outdir}/includes"
                excludesfile="${outdir}/excludesfile"
                patternsetid="easyshare.versus.jsf">
    </nx:zipdiff>
    <unzip src="${outdir}/nxr-${marketplace.easyshare.version}.zip" dest="${outdir}/marketplace/install">
      <patternset refid="easyshare.versus.jsf" />
    </unzip>

    <!-- Generate install.xml file -->
    <var name="install.content" value="&lt;install&gt;" />
    <var name="install.content"
         value="${install.content}${line.separator}
    &lt;update file=&quot;${package.root}/install/bundles&quot; todir=&quot;${env.bundles}&quot; /&gt;" />
    <if>
      <available file="${outdir}/marketplace/install/lib" />
      <then>
        <var name="install.content"
             value="${install.content}${line.separator}
    &lt;update file=&quot;${package.root}/install/lib&quot; todir=&quot;${env.lib}&quot; /&gt;" />
      </then>
    </if>
    <var name="install.content"
         value="${install.content}${line.separator}
    &lt;copy file=&quot;${package.root}/install/artifacts-easyshare.properties&quot;${line.separator}
      todir=&quot;${env.server.home}/sdk&quot; overwrite=&quot;true&quot; /&gt;${line.separator}
    &lt;copy file=&quot;${package.root}/install/test-artifacts-easyshare.properties&quot;${line.separator}
      todir=&quot;${env.server.home}/sdk&quot; overwrite=&quot;true&quot; /&gt;${line.separator}" />
    <var name="install.content" value="${install.content}${line.separator}&lt;/install&gt;${line.separator}" />
    <echo file="${outdir}/marketplace/install.xml" message="${install.content}" />

    <zip destfile="${outdir}/${maven.project.artifactId}-${marketplace.easyshare.version}.zip"
         basedir="${outdir}/marketplace" />
    <artifact:attach file="${outdir}/${maven.project.artifactId}-${marketplace.easyshare.version}.zip" type="zip" />

    <!-- cleanups -->
    <delete dir="${outdir}/marketplace" failonerror="false" />
    <delete dir="${outdir}/nxr" failonerror="false" />
    <delete dir="${outdir}/nuxeo-nxr-server-jsf-ui" failonerror="false" />
    <delete file="${outdir}/nuxeo-nxr-server-${nuxeo.distribution.version}.zip" failonerror="false" />
    <delete file="${outdir}/nuxeo-nxr-jsf-ui-${nuxeo.distribution.version}.zip" failonerror="false" />
    <delete file="${outdir}/nuxeo-nxr-server-jsf-ui-${nuxeo.distribution.version}.zip" failonerror="false" />
    <delete file="${outdir}/nxr-${maven.project.version}.zip" failonerror="false" />
  </target>

</project>
